import cssContentContainer from "../assets/css/auth/content-container.module.css";

const Discussion = (props) => {
    return (
        <div id = "page==discussion" className = { cssContentContainer["content-container"] }>
            
        </div>
    );
}

export default Discussion;